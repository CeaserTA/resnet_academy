# Project Tracker — Resnet LMS

Living backlog, organized by build phase. Each phase is roughly sequential (later phases depend
on earlier ones), but within a phase, tasks can run in parallel across backend/frontend.

**Status legend:** `Not started` · `In progress` · `Blocked` · `Done`

Update this file as work lands — it's the map back to `PRD_Online_Study_Platform.md`,
`schema.sql`, and `architecture.md` for anyone joining mid-project.

---

## Phase 0 — Foundation

Infrastructure and scaffolding nothing else can start without.

| ID | Task | Reference | Depends on | Status |
|---|---|---|---|---|
| 0.1 | Repo scaffold: Laravel app + React app, CI pipeline (Pint, Larastan, ESLint, Prettier, tests) | `code-standards.md` §5 | — | In progress — Laravel app scaffolded (Breeze api stack, Sanctum, Socialite, Pest, Larastan all installed and green locally). React app scaffolded in `frontend/` (Vite + React 19 + TypeScript strict, Tailwind v4 themed from `ui-context.md`, React Router, TanStack Query, React Hook Form + Zod, Axios, ESLint + `react-hooks` as errors + Prettier, Vitest + React Testing Library) — all green locally (`npm run build`/`lint`/`test`). CI pipeline (GitHub Actions or similar) for either app not started |
| 0.2 | Run initial migrations from `schema.sql` (Phase 0–1 tables only: `users`, `categories`, `courses`, `course_instructors`) | `schema.sql` §1–2 | 0.1 | Done — went further than the Phase 0–1 subset: all ~53 tables in `schema.sql` migrated now (including new `oauth_accounts`), since the schema is fully specified and stable; avoids a migration-catchup step for later phases |
| 0.3 | Auth (Laravel Sanctum), role model (admin/instructor/student) | `architecture.md` §4 | 0.2 | Done — Breeze (api stack) + Sanctum cookie/session auth, Socialite Google OAuth linking via `oauth_accounts`, `UserRole`/`UserStatus` enums, `User implements MustVerifyEmail` |
| 0.4 | Base Policy scaffolding per role | `code-standards.md` §2 | 0.3 | Done — `CoursePolicy`, `EnrolmentPolicy`, `CategoryPolicy`, `UserPolicy` |
| 0.5 | Environment config: MySQL, Redis, S3-compatible storage, Bunny Stream, email provider — dev + staging | `architecture.md` §2 | 0.1 | In progress — local MySQL configured and running (dev + testing databases); Redis not installed locally, using Laravel's `database` queue/cache driver as a local substitute (same job code, swap `QUEUE_CONNECTION=redis` for staging/prod); S3/Bunny Stream/Resend are env-key placeholders only, not yet wired to real accounts; no staging environment yet |
| 0.6 | Deployment pipeline: app servers, queue workers, scheduler | `architecture.md` §6 | 0.5 | Not started |
| 0.7 | API documentation (OpenAPI/Swagger) | — | 1.1–1.8 | Done — `dedoc/scramble` installed, auto-generates the OpenAPI 3.1 spec from existing FormRequests/Resources/route model binding (no manual annotations needed); served at `/docs/api` (Swagger-style UI, open in local env) and `/docs/api.json`; documents auth model (Sanctum session cookie, not bearer) and all current endpoints |
| 0.8 | Normalized JSON error envelope (`architecture.md` §4: `{error:{code,message,fields}}`) | `architecture.md` §4 | 0.1 | Done — found this was specified but never implemented; added `bootstrap/app.php` exception renderers (validation/auth/authorization/not-found/generic HTTP) so every JSON error follows one shape the frontend's `ApiError` class parses generically |

---

## Phase 1 — Catalogue & Enrolment

| ID | Task | Reference | Depends on | Status |
|---|---|---|---|---|
| 1.1 | Course/category CRUD (admin) | FR-1 | 0.4 | Done — `CategoryController`, `CourseController`, admin-only via policies, instructor can update own courses |
| 1.2 | Course catalogue browse + filters (category, level, schedule, instructor) | FR-1 | 1.1 | Done — public browse defaults to published-only, paginated; admin/instructor see draft/own courses too |
| 1.3 | Course apply/enrol endpoint — auto-confirm, no eligibility gate | FR-2, FR-3 | 1.2 | Done — `EnrolmentController@store` via `EnrolmentService`, always `status = confirmed` |
| 1.4 | `confirmation_delay_hours` per course, configurable in course settings | Business rule 2 | 1.3 | Done — read from `course.confirmation_delay_hours` at enrolment time, never hardcoded |
| 1.5 | Queued job: delayed confirmation email | FR-4, `architecture.md` §5.1 | 1.4, 0.5 | Done — `SendEnrolmentConfirmationEmail` (delayed dispatch, `ShouldBeUnique`) + `enrolments:send-due-confirmation-emails` scheduled sweep as a fallback |
| 1.6 | Admin bulk/CSV enrolment import | Business rule 1 | 1.3 | Done — `EnrolmentImportController` queues `ImportEnrolmentsFromCsv`, `BulkEnrolmentImporter` skips already-enrolled students |
| 1.7 | `orders` entity + course pricing display (no gateway integration yet) | Business rule "Payment/billing" | 1.1 | Done — `orders` row (`status = pending`) created alongside every enrolment; `CourseResource`/`OrderResource` expose price |
| 1.8 | Tests: auto-confirm never rejects, delay is per-course, bulk import doesn't duplicate enrolments | `code-standards.md` §2 (Testing) | 1.3–1.6 | Done — 28 Pest tests passing (auth flows, catalogue filters/authorization, enrolment business rules, bulk-import idempotency, admin user management) |
| 1.9 | Admin-only user directory (`GET /admin/users`, optional `?role=`) | needed to populate the frontend's instructor picker on course create/edit | 1.1 | Done — small backend addition surfaced by building the frontend; `UserPolicy::viewAny` (admin-only) |
| 1.10 | Frontend: auth screens (login, register, forgot/reset password, verify-email notice, Google OAuth button) | `architecture.md` §4, `ui-context.md` | 1.1–1.9, 0.3 | Done — `frontend/src/features/auth/` — Sanctum cookie/CSRF flow wired through `lib/api/client.ts` + `lib/auth/AuthContext.tsx`; verified end-to-end against the real backend (curl, not just unit tests — see §"Manual verification" note below) |
| 1.11 | Frontend: course catalogue (browse/filter/detail) + student enrolment (My courses) | FR-1, FR-2 | 1.10 | Done — `frontend/src/features/catalogue/`, `frontend/src/features/enrolment/`; status badges map 1:1 to the backend enums via `lib/statusBadge.ts`, never inventing synonyms |
| 1.12 | Frontend: admin console (course/category CRUD, bulk CSV import, instructor/admin provisioning) | 1.1, 1.6 | 1.10 | Done — `frontend/src/features/admin/`; role-aware `AppShell` nav + `ProtectedRoute` (UX convenience only — every write is still re-checked server-side via Policies) |

---

## Phase 2 — Course Structure & Content Delivery

| ID | Task | Reference | Depends on | Status |
|---|---|---|---|---|
| 2.1 | Modules CRUD, ordering | FR-6 | 1.1 | Done — `ModuleController`, admin or the course's own instructor via `ModulePolicy`; `order_index` auto-increments if omitted |
| 2.2 | Groups/cohorts CRUD, module-group scoping | FR-7 | 2.1 | Done — `GroupController` (+ member add/remove), `Module::groups()` sync via `group_ids` on module create/update; a module with no linked groups applies to every student |
| 2.3 | Scheduled release (`scheduled_start_at`) | FR-8 | 2.1 | Done — enforced in `ProgressEngine::evaluateCourseUnlocks()`, not just stored |
| 2.4 | `module_items` ordering/required-flag layer | `schema.sql` §9 | 2.1 | Done — `ModuleItemController@update` (reorder/toggle `is_required`); resource creation auto-creates its `module_items` row (one action instead of two) |
| 2.5 | Resource type: video (Bunny Stream upload + playback + captions) | FR-10 | 2.4, 0.5 | Partial — DB layer + validation + completion signal (≥90% watched) all done via `ResourceManager`/`ProgressEngine`; actual Bunny Stream API orchestration (video object creation, signed upload URLs) deferred — no real Bunny credentials yet, same scoping decision as payments. Endpoint currently accepts an already-uploaded `bunny_stream_video_id` |
| 2.6 | Resource type: document/slides (PDF/PPTX/DOCX) | FR-10 | 2.4 | Done |
| 2.7 | Resource type: reading/text lesson (rich text) | FR-10 | 2.4 | Done |
| 2.8 | Resource type: external link | FR-10 | 2.4 | Done |
| 2.9 | Resource type: SCORM/xAPI package | FR-10 | 2.4 | Done — DB/validation/CRUD only; no in-house SCORM/xAPI runtime (out of scope). Completion signal reuses "mark as read" since the PRD doesn't define one for this type — documented in `ProgressEngine::isResourceComplete()` |
| 2.10 | Resource type: live session (Zoom/Meet link + scheduling) + attendance marking | FR-10 | 2.4 | Done — `POST /resources/{resource}/progress/attendance` marks `live_session_attendance` |
| 2.11 | Resource type: downloadable file | FR-10 | 2.4 | Done — completion signal reuses "opened", same reasoning as SCORM above |
| 2.12 | Progress Engine: sequential module locking (unlock evaluation) | FR-9, `architecture.md` §5.2 | 2.3, 2.4 | Done — `ProgressEngine::evaluateCourseUnlocks()`, run on enrolment, on-demand (`GET /courses/{course}/progress`), and on a schedule (`progress:evaluate-module-unlocks`, every 5 min) |
| 2.13 | Progress Engine: per-resource-type completion signal (video %, mark-as-read, opened) | "Module completion definition" | 2.5–2.11 | Done — `ProgressEngine::isResourceComplete()`/`recordVideoPing()`/`markRead()`/`markOpened()`/`markAttendance()`; module rollup checks only `is_required = true` items |
| 2.14 | Course versioning: change-log on edit, notify enrolled students | Business rule "Course versioning" | 2.1 | Done — change-log already existed from Phase 1; added `NotificationDispatcher::notifyCourseChanged()` (in-app `notifications` row per confirmed enrolment) to close the "notify" half. Full notification center (read/unread inbox API, email/SMS/push fan-out) is still Phase 5 |
| 2.15 | Tests: locking requires both schedule + prior completion; optional resources don't block completion | `code-standards.md` §2 | 2.12, 2.13 | Done — 19 new Pest tests (`ModuleTest`, `ResourceTest`, `ModuleLockingTest`, `CompletionTest`, `UserManagementTest`), including the exact rule from this doc's own example: "locks module 2 until module 1 is completed even if scheduled_start_at has passed" |
| 2.16 | Frontend: admin course builder (modules, group/cohort scoping, all 7 resource types) | 2.1–2.11 | 1.10–1.12 | Done — `frontend/src/features/courseStructure/` (`CourseBuilderPage`, `ModuleCard`, `ResourceForm`, `GroupsPanel`); group-member picker uses a raw student-ID input rather than a search/typeahead (no general "list students" endpoint open to instructors yet) — flagged as a follow-up, not a blocker |
| 2.17 | Frontend: student course player + resource consumption (video/reading/link/document/scorm/live-session/download) with progress actions | 2.12–2.13 | 1.10–1.12 | Done — `frontend/src/features/learning/` (`CoursePlayerPage` shows locked modules dimmed per `ui-context.md` §6, `ResourceViewerPage` per-type rendering + mark-read/mark-opened/mark-attendance/video-watch actions). Video playback is a simulated player (play/pause advances a timer, sends the same watch-progress pings a real player would) since Bunny Stream isn't wired up yet (2.5) — the completion logic being exercised is real, only the video surface is a stand-in |
| 2.18 | Frontend tests + end-to-end verification | 2.16–2.17 | — | Done — 3 new Vitest suites (`ResourceForm`, `CoursePlayerPage`, updated total 12 passing); full manual curl-driven flow re-verified end-to-end on the real backend (admin creates module → creates reading resource → student sees it unlocked → marks read → module rolls up to `completed`) after an unrelated local MySQL service outage was resolved mid-session |

---

## Phase 3 — Assessment

| ID | Task | Reference | Depends on | Status |
|---|---|---|---|---|
| 3.1 | Assignments CRUD, submission (file/text) | FR-11 | 2.4 | Done — `AssignmentManager` (create/update/delete mirrors `ResourceManager`'s create-with-module-item pattern), `AssignmentSubmissionService::submit()`; `StoreSubmissionRequest` conditionally requires `file_url`/`text_content` based on `assignment.submission_type` |
| 3.2 | Instructor grading + feedback | FR-11 | 3.1 | Done — `AssignmentSubmissionService::grade()`; `final_score = round(raw_score * (1 - late_penalty_percent/100), 2)`; gated by `AssignmentPolicy::grade` (admin or course-teaching instructor) |
| 3.3 | Grading rubrics | Business rule "Grading rubrics" | 3.1 | Done — `AssignmentManager::syncRubrics()` (replace-all semantics on update), `AssignmentSubmissionRubricScore` rows replaced wholesale on each `grade()` call |
| 3.4 | Tiered late-penalty policy engine (0–24h/24–48h/48h+) | Business rule "Late submission policy" | 3.1 | Done — `LatePenaltyCalculator::penaltyPercentFor()`, bands fully configurable per-policy via `late_penalty_tiers` (never hardcoded); default "Standard Late Policy" (0–24h -10%, 24–48h -25%, 48h+ -50%) seeded in `DatabaseSeeder` |
| 3.5 | Plagiarism check integration (external service) | Business rule "Plagiarism detection" | 3.1 | Not started — `plagiarism_check_enabled` flag and `PlagiarismReport` model/table exist from Phase 0, but no external scanning service is wired up; deferred, no vendor chosen |
| 3.6 | Question banks + question CRUD (MCQ single/multi, true/false, short answer, essay) | FR-12 | 2.4 | Done — `QuestionBankController`/`QuestionController`, `QuestionManager::create()` derives `auto_gradable` server-side from `type` (never trusts client input) |
| 3.7 | Evaluations: attempt limits, time limits, randomization, pass score | FR-12, Business rule "Attempt limits" | 3.6 | Done — `EvaluationManager` (CRUD + question-set sync via pivot `order_index`), `EvaluationAttemptService::start()` enforces `max_attempts`/`available_from`/`available_until`, `submit()` enforces `time_limit_minutes` against `started_at` |
| 3.8 | Auto-grading for objective question types | FR-12 | 3.7 | Done — `EvaluationAttemptService::isObjectiveAnswerCorrect()` (exact-set match on selected vs. correct option IDs) for mcq_single/mcq_multi/true_false; student-facing `AttemptQuestionResource` never exposes `is_correct` |
| 3.9 | Manual grading queue for short-answer/essay | FR-12 | 3.7 | Done — `EvaluationAttemptService::submit()` flags `needsManualGrading` and leaves status `submitted`; `gradeManualAnswers()` (instructor/admin only, `EvaluationPolicy::grade`) scores each answer then finalizes |
| 3.10 | Progress Engine: assignment completes on submit, evaluation completes on pass | "Module completion definition" | 3.1, 3.7 | Done — `AssignmentSubmissionService::submit()` calls `ProgressEngine::rollupModuleCompletion()` immediately (grading never re-triggers it); `EvaluationAttemptService::finalizeScore()` only calls it when `passed = true` — a later failing retake cannot un-complete an already-passed module item, since `ProgressEngine::isModuleItemComplete()` checks for the *existence* of any passed attempt |
| 3.11 | Gradebook: aggregated view, weighting toward final grade | Business rule "Gradebook" | 3.2, 3.8 | Done — `GradebookService::forCourse()`, `GET /courses/{course}/gradebook` (`CoursePolicy::viewGradebook`). No explicit weighting field exists in `schema.sql`, so weighting is expressed as: each assignment's `max_score` in points, each evaluation nominally worth 100 points (its `score_percent`); `final_grade_percent = earned / possible * 100`. Latest submission per assignment, best graded attempt per evaluation |
| 3.12 | Tests: late-penalty bands apply correctly at boundary hours; quiz retake doesn't lower pass bar | `code-standards.md` §2 | 3.4, 3.7 | Done — 16 new Pest tests (`LatePenaltyCalculatorTest`: all 3 bands incl. exact 24h/48h boundaries and the on-time/no-policy zero-penalty cases; `AssignmentSubmissionTest`: tiered penalty on submit, penalty applied at grading, completes-on-submit-not-grading; `EvaluationAttemptTest`: auto-grading, retake-doesn't-lower-the-bar, `max_attempts` enforcement, answer-key-never-leaks; `GradebookTest`). Found and fixed two real bugs along the way: (1) `EvaluationAttempt` model declared `UPDATED_AT = null` but the table has no timestamp columns at all, so `create()` failed — needed `$timestamps = false` instead, matching `AssignmentSubmission`/`EvaluationAttemptAnswer`; (2) `EvaluationController::show()` had no authorization check at all and eager-loaded `questions.options` — any authenticated student could hit `GET /evaluations/{id}` and read the full answer key via `QuestionOptionResource.is_correct`. Fixed with a new `EvaluationPolicy::view` (admin/course-teaching-instructor only); students now get evaluation metadata via `EvaluationAttemptController::start()`'s safe summary instead |
| 3.13 | Backend: unify a module's `items` (resources + assignments + evaluations) into one ordered list | Discovered while building 3.14 — `ModuleController::index` previously only returned `resources`, so the course player had no way to reach assignments/evaluations at all | 3.1, 3.7, 2.13 | Done — `Module::assignments()`/`evaluations()` relations added; `ModuleResource::itemsPayload()` merges all three via `AssignmentItemResource`/`EvaluationItemResource` (new, summary-only — no rubrics/questions/answer key) sorted by `module_items.order_index`; `EvaluationAttemptController::index()` (list attempts for an evaluation) added for the grading queue, which hadn't been wired at all |
| 3.14 | Frontend: assignment submission + instructor grading (rubrics), evaluation-taking + manual-grading queue, gradebook | 3.1–3.11, 3.13 | 1.10–1.12, 2.16–2.18 | Done — `frontend/src/features/assessment/` (`AssignmentSubmitPage`/`AssignmentGradingPage`, `EvaluationTakePage` with a live countdown that auto-submits at the time limit, `EvaluationGradingPage` for short-answer/essay scoring, `GradebookPage` per `ui-context.md` §6 table styling). `CoursePlayerPage`/`ModuleCard` updated to route by `item_type` instead of assuming every module item is a resource. `AssignmentQuickForm`/`EvaluationQuickForm` cover shell creation (title, scoring, limits) — question-bank/question authoring (banks, MCQ options, marking correct answers) and rubric-criteria editing have no UI yet, API-only; same open-question shape as "Content authoring model" below |
| 3.15 | Frontend tests + verification | 3.14 | — | Done — 1 new Vitest suite (`EvaluationTakePage`: answers a question, confirms no answer key ever renders, submits, sees pass/fail), total 13 passing; `tsc -b` and `eslint .` both clean. Full backend Pest suite re-verified after the 3.13 unification + 3.12's security fix: 63/63 passing. No browser tool is available in this environment, so the new pages were **not** click-tested in an actual browser — verification here is typecheck + lint + Vitest + the backend contract tests (Pest) confirming the exact JSON shapes each page consumes, not a live UI walkthrough |

---

## Phase 4 — Progress, Certification & Payments Foundation

| ID | Task | Reference | Depends on | Status |
|---|---|---|---|---|
| 4.1 | Student-facing progress dashboard (per course/module status) | FR-13 | 2.12, 2.13 | Done — `GET /me/progress` (`ProgressController::dashboard()`), one row per confirmed enrolment: course-level `status` (not_started/in_progress/completed) and `percent_complete` derived from the same `module_progress` rows `courseProgress()` already exposed per-course, plus the certificate if issued. Frontend: `MyCoursesPage` cards now show a progress bar, status badge, and certificate download link |
| 4.2 | Certificate generation + issuance on course completion | `architecture.md` §5.4 | 3.10 | Done — `CertificateService::issueForCourseCompletion()` (idempotent via `firstOrCreate` on the `student_id`+`course_id` unique constraint), wired into `ProgressEngine::rollupModuleCompletion()`: after a module completes and unlocks are re-evaluated, if that module was the *last* in `applicableModules()` for the student, issue the certificate. PDF rendered off the request cycle by `GenerateCertificatePdf` (`ShouldBeUnique`/`ShouldQueue`) via `barryvdh/laravel-dompdf` (newly installed — pure-PHP, no external account needed, unlike Bunny/S3/Resend) against a Blade template (`resources/views/certificates/pdf.blade.php`, used purely as a PDF renderer, not a served page — doesn't conflict with architecture.md §2's "no Blade for the product"), stored on the `public` disk. `NotificationDispatcher::notifyCertificateIssued()` fires alongside |
| 4.3 | Certificate download / verification view | — | 4.2 | Done — `GET /certificates` (mine), `GET /certificates/{certificate}` (`CertificatePolicy::view` — owner or admin), and a public, unauthenticated `GET /certificates/verify/{certificateNumber}` returning a deliberately minimal shape (`CertificateVerificationResource` — name/course/date only, no email/IDs). Frontend: `CertificateVerifyPage` at `/verify-certificate` (public, linked from the site footer), download links on `MyCoursesPage` |
| 4.4 | Attendance tracking rollup for live sessions | Business rule "Attendance tracking" | 2.10 | Done — `GET /resources/{resource}/attendance` (`ProgressController::attendanceRoster()`, `ResourcePolicy::viewAttendance` — admin/course-teaching-instructor), cross-references every confirmed-enrolled student against `live_session_attendance` so absentees show up too, not just attendees. 422 if the resource isn't a `live_session`. Frontend: `AttendanceRosterPage`, linked from a new roster icon on `live_session` rows in `ModuleCard` |
| 4.5 | Tests: certificate issues exactly once per student per course | `code-standards.md` §2 | 4.2 | Done — 12 new Pest tests across `CertificateIssuanceTest` (issues on last-module completion, doesn't issue while a later module is incomplete, exactly-once even when triggered twice — direct test of the `firstOrCreate` guarantee), `CertificateVerificationTest` (public access, 404 on unknown number, ownership-scoped list/show), `ProgressDashboardTest`, `AttendanceRosterTest`. Also cleaned up `Certificate`/`LiveSessionAttendance` models (added `HasFactory` + relation generics — same Phase-0-era gap fixed for assessment models in Phase 3) and added the missing `CertificateFactory`/`LiveSessionAttendanceFactory` and a `liveSession()` state on `ResourceFactory`. Full regression: 75/75 Pest passing, Pint/Larastan clean |
| 4.6 | Frontend tests + verification | 4.1–4.4 | — | Done — 1 new Vitest suite (`CertificateVerifyPage`: types a number, submits, sees the verified name/course), total 14 passing; `tsc -b` and `eslint .` both clean |

---

## Phase 5 — Communication

| ID | Task | Reference | Depends on | Status |
|---|---|---|---|---|
| 5.1 | Conversations/messages: Admin↔Instructor | FR-15 | 0.3 | Done — one generic conversations/messages system for all three pairings (schema.sql's own design note: "just users with different roles"), not three separate features. `ConversationService::canConverseWith()` is the single rule: any pair involving Admin is always allowed |
| 5.2 | Conversations/messages: Instructor↔Student | FR-16 | 0.3 | Done — `ConversationService::instructorTeachesStudent()` scopes this to students confirmed-enrolled in a course the instructor teaches (an unscoped free-for-all felt like an obvious spam vector, and nothing in the PRD suggested otherwise) |
| 5.3 | Conversations/messages: Admin↔Student | FR-17 | 0.3 | Done — covered by the same generic rule as 5.1. Student↔Student is deliberately never allowed here — that's what forums (5.6) are for, per schema.sql |
| 5.4 | Read receipts | Business rule "Read receipts" | 5.1–5.3 | Done — `messages.read_at` is a single timestamp, which only works cleanly because conversations are always exactly 2 participants in this MVP (no group messaging); `ConversationController::show()` marks every unread message from the other participant read on open. "Online presence" (named in the same business-rule bullet) has no backing column/table anywhere in schema.sql — flagged as a real schema gap, not attempted |
| 5.5 | Student support tickets (raise/respond/resolve) | 3.5 (User Roles table) | 0.3 | Done — `TicketService` (create/reply/update), student-only creation, `TicketPolicy::manage` scopes status/assignment changes to admins, the assigned staff member, or an instructor teaching the ticket's course |
| 5.6 | Course forums: threads, posts, search | FR-18 | 1.1 | Done — redesigned post-MVP from a thread-list UI into a social feed (Feed / My Posts tabs, per the user's mockups) with post attachments. Kept the underlying `ForumThread`+`ForumPost` schema rather than restructuring it: a "feed post" is a thread + its oldest post (`ForumThread::headPost()`, an `oldestOfMany()` relation), and "replies" are every other post in the thread — `forum_threads.title` still exists (schema keeps it NOT NULL) but is now auto-generated from the body and never shown. Added `attachment_type`/`attachment_path`/`attachment_original_name` to `forum_posts` (migration `add_attachment_columns_to_forum_posts_table`, additive, documented in `schema.sql`) — image/video/audio are real uploads (`ForumService::storeAttachment()`, `public` disk, 5MB cap enforced server-side via `StoreForumThreadRequest`/`UpdateForumPostRequest` and client-side in `ForumComposer`); "Article" is a long-form text mode with no file, confirmed with the user. Editing (`ForumPostController::update()`, `ForumPostPolicy::update()` — author-only) and deleting (`ForumService::deletePost()` — deleting a head post cascades the whole thread via the existing FK, deleting a reply removes just that post) are both new. `?mine=1` on the index endpoint powers the My Posts tab. Search now matches post bodies only (MySQL `FULLTEXT`, boolean mode — natural-language mode was found to silently exclude any word appearing in ≥50% of a course's posts); title-based search was dropped since title is no longer user-facing. FR-18 says "Student↔Student" but nothing gates posting to students-only; instructors/admins can post and reply too |
| 5.7 | Forum moderation: report/pin/lock | Business rule "Forum moderation" | 5.6 | Done — `ForumPostReportController` (report/moderation-queue/resolve, admin/course-instructor only) unchanged. Pin/lock (`ForumThreadController::update()`) moved from a dedicated thread page into each feed card's "•••" menu (staff-only) as part of the 5.6 redesign; locking still blocks all new replies including from staff. The standalone `ForumThreadPage`/`/forum-threads/:id` route was removed — cards now handle viewing, replying, editing, deleting, reporting, and moderating inline, with nothing else linking to that route. Forum redesign regression: `ForumTest` rewritten (13 tests: attachments, edit, delete-cascades-thread vs delete-reply-only, `mine` filter, search, plus the pre-existing lock/pin/report coverage), new `ForumComposer`/`ForumPostCard` Vitest coverage; full regression 120/120 Pest, 20/20 Vitest (13 files), Pint/Larastan/`tsc -b`/eslint all clean |
| 5.8 | Announcements (broadcast, course-scoped) | §3.6 last bullet | 1.1 | Done — `AnnouncementController`, instructor/admin only, fans out an in-app notification to every confirmed-enrolled student via `NotificationDispatcher::notifyAnnouncementPosted()` (same shape as Phase 2's `notifyCourseChanged`) |
| 5.9 | Notifications system: in-app + email, event triggers (due soon, grade posted, forum reply, module unlocked) | Business rule "Notifications system" | 3.1, 3.7, 2.12, 5.6 | Partial — the read/unread in-app inbox is done (`NotificationController`: list with unread count, mark-one-read, mark-all-read; `NotificationBell` dropdown in the app shell). Event triggers wired: new message, ticket reply, forum reply, announcement posted, grade posted (`AssignmentSubmissionService::grade()`, `EvaluationAttemptService::finalizeScore()`), module unlocked (`ProgressEngine::evaluateCourseUnlocks()`, exactly once per lock→unlocked transition). **Not done**: "assignment due soon" — needs a new scheduled command (check confirmed-enrolled students with an unsubmitted assignment due within some window, with dedup so it doesn't re-notify every sweep) that didn't feel like it belonged in the same pass as the interactive features; deferred as a clearly-scoped follow-up. Email/SMS/push fan-out remains unbuilt — every notification is `NotificationChannel::InApp` only, as already flagged when the dispatcher was first built in Phase 2 |
| 5.10 | (Optional) real-time delivery for messages/forum replies | `architecture.md` §2 | 5.1–5.9 | Not started — explicitly optional; the notification bell polls via TanStack Query's normal refetch behavior rather than a websocket |
| 5.11 | Tests + verification | 5.1–5.9 | — | Done — 26 new Pest tests (`ConversationTest`, `TicketTest`, `ForumTest`, `AnnouncementTest`, `NotificationTest`, plus module-unlock-notifies and grade-posted-notifies assertions added to existing tests). Found and fixed two real bugs along the way: (1) `ForumPostReportController::store()` didn't explicitly set `status`, so the in-memory model's cast enum was null until a reload — same "DB-default column omitted from `create()`" gotcha hit repeatedly in earlier phases; (2) natural-language `whereFullText` search (see 5.6). Also added the missing `Course::announcements()`/`tickets()` relations and a `ResourceFactory::liveSession()` state. Frontend: 1 new Vitest suite (`NotificationBell`), found and fixed a real `react-hooks/set-state-in-effect` lint violation in `ComposeMessageForm` (was syncing default form state from a query result inside a `useEffect` — replaced with a derived value, no effect needed) and a test regression in `CoursePlayerPage.test.tsx` caused by embedding `AnnouncementsPanel` (needed `AuthProvider` in the test tree). Full regression: 100/100 Pest, 15/15 Vitest, Pint/Larastan/tsc/eslint all clean |

---

## Phase 6 — Analytics, Audit & Non-functional

| ID | Task | Reference | Depends on | Status |
|---|---|---|---|---|
| 6.1 | Engagement event ingestion | Business rule "Analytics dashboard" | 1.3, 2.13, 3.10 | Done — `EngagementTracker::track()` (`app/Services/Analytics/EngagementTracker.php`) is the single write path for `engagement_events`, wired into `ProgressEngine` (`recordVideoPing`/`markRead`/`markOpened`/`markAttendance` → `resource_viewed`), `AssignmentSubmissionService::submit()` (`assignment_submitted`), `EvaluationAttemptService::submit()` (`quiz_attempted`). `login` deliberately not tracked here — `engagement_events.course_id` is `NOT NULL` and a login has no course context, documented in the service's docblock rather than guessed around. Also fixed `EngagementEvent`/`AuditLog` models (missing `HasFactory` + relation generics, a Phase-0-era gap like the ones fixed for other models in earlier phases) and added their factories |
| 6.2 | Admin/Instructor analytics dashboard: completion rates, at-risk flags, engagement | Business rule "Analytics dashboard" | 6.1 | Done — `AnalyticsService::courseAnalytics()` (`app/Services/Analytics/AnalyticsService.php`): completion rate from `Certificate` rows, at-risk flagging with two independent thresholds (7-day enrolment grace period before a student can even be flagged, 14-day inactivity window since last `engagement_events` row), students with a certificate are never at-risk regardless of recent inactivity, 30-day rolling engagement-type counts. `AnalyticsController::courseAnalytics()`, gated by `CoursePolicy::viewAnalytics` (admin or the course's teaching instructor). Frontend: `AnalyticsDashboardPage` at `/admin/courses/:id/analytics`, linked from `CourseBuilderPage` |
| 6.3 | Audit logging on sensitive mutations (grade changes, enrolment status, role changes) | Business rule "Audit logging" | 0.3 | Done — grade changes: `AssignmentSubmissionService::grade()` and `EvaluationAttemptService::gradeManualAnswers()` log `grade.changed` (deliberately only the human-driven manual-grading paths, not `finalizeScore()`'s auto-grading — "who changed a grade" implies human accountability, not every auto-graded quiz). Enrolment status: new `EnrolmentService::withdraw()` + `EnrolmentPolicy::withdraw` (admin or the enrolment's own student), route `POST /enrolments/{enrolment}/withdraw` — this mutation didn't exist before this phase. Role/status changes: new `AdminUserController::update()` + `UserPolicy::update` (admin only, self-change blocked with a 422 so an admin can't lock themselves out), route `PATCH /admin/users/{user}` — also didn't exist before. Read side: `GET /admin/audit-logs` (`AuditLogController`, filters by `entity_type`/`entity_id`/`action`, admin-only via `UserPolicy::viewAny`) |
| 6.4 | Accessibility pass: WCAG 2.1 AA (captions, screen-reader support) | Business rule "Accessibility" | 2.5–2.11, frontend components | Done — audited every icon-only button across `frontend/src` for `aria-label` coverage; found and fixed 3 real gaps (`ModuleCard`'s expand/collapse chevron toggle, `CourseListPage`'s edit button, `GroupsPanel`'s delete-group button) and confirmed the rest (`NotificationBell`, `ForumThreadPage`'s pin/lock/report/delete actions, every other delete button) already had one |
| 6.5 | Mobile responsiveness pass | Business rule "Mobile responsiveness" | frontend components | Done — the 4 data tables (`CourseListPage`, `AttendanceRosterPage`, `GradebookPage`, `AuditLogPage`) already wrapped in `overflow-x-auto`. Found and fixed 5 dense forms with hardcoded `grid-cols-2`/`grid-cols-3` that didn't stack on narrow screens (`ResourceForm`, `EvaluationQuickForm`, `AssignmentQuickForm`, `CourseFormPage` — switched to `grid-cols-1 sm:grid-cols-N`) plus the admin team-member role/status row (`ProvisionUserPage`) which now stacks vertically below `sm:` |
| 6.6 | Data privacy/compliance review (local data protection law) | Business rule "Data privacy/compliance" | all data-handling features | Done — reviewed `architecture.md` §7 against actual data handling: `EngagementTracker`'s `event_meta` is already data-minimized (resource id/type only, no PII). Built the export/delete-on-request capability §7 flags as missing but not detailed by the PRD: new `AccountController::export()` (`GET /me/data-export`, a downloadable JSON of the user's own enrolments/certificates/submissions/messages/forum posts/tickets/notifications/engagement events, audited as `account.data_exported`) and `::requestDeactivation()` (`POST /me/request-deactivation`, registered in `routes/web.php` under `web` middleware alongside `/logout` since it touches the session the same way — a soft, admin-reversible deactivation rather than a hard delete, since schema.sql's foreign keys mean a real hard delete would cascade into grades/enrolments other users still legitimately need; reasoning documented in the controller's docblock). Frontend: new `AccountPage` at `/account`, linked via a shield icon next to the notification bell in both the desktop sidebar and mobile header. **Full Phase 6 regression**: 113/113 Pest passing, 17/17 Vitest passing (11 files), Pint/Larastan/`tsc -b`/`eslint` all clean |

---

## Phase 7 — Post-MVP

Explicitly deferred by the PRD; tracked so they aren't lost.

| ID | Task | Reference | Status |
|---|---|---|---|
| 7.1 | Payment gateway integration against `orders` | Business rule "Payment/billing" | Not started — 7.9 added a real student-submits-a-receipt / admin-confirms workflow that moves an order through `pending`→`partial`→`paid`, but nothing here talks to an actual payment gateway/provider; a student's "payment" is still a manually-reviewed claim, not a processed transaction |
| 7.2 | Capacity-based waitlisting | FR-5 note | Not started |
| 7.3 | In-browser lesson/quiz authoring builder (vs. raw file upload) | Open question "Content authoring" | Not started |
| 7.4 | Admin dashboard redesign: system-wide stat cards, quick actions, recent activity, consolidated top bar | User request (post-MVP polish) | Done — `/dashboard` for admins previously just rendered the bare course list with nothing else; now a real dashboard. New `AnalyticsService::systemSummary()` (students/instructors/courses-by-status/confirmed enrolments/certificates issued/revenue **grouped by currency**, not a naive cross-currency sum/open tickets/system-wide at-risk count/recent audit log entries), `Admin\DashboardController::summary()` at `GET /admin/dashboard-summary`, admin-only via the same `UserPolicy::viewAny` gate as the audit log. Frontend: new `AdminDashboardPage` (full-width `max-w-7xl`, not a narrow centered column — the pattern that was causing dead whitespace on wide screens) with an 8-card responsive stat grid, a quick-actions row to the screens admins reach for most, and a recent-activity feed; wired in as the admin's `/dashboard` landing content in `DashboardPage.tsx` (instructor/student dashboards untouched). `AppShell.tsx` consolidated the previously-duplicated notification bell/account icons (one copy in the desktop sidebar footer, another in a mobile-only header) into one `TopBar` rendered at every breakpoint, plus a new `ProfileMenu` dropdown (name/email, account link, log out) replacing the separate icons — also promoted `components/ui/Avatar.tsx` reuse into it. Scope was explicitly limited to this core redesign; dark mode, global search, and CSV export were proposed and deliberately deferred per the user's choice. Regression: 124/124 Pest, 26/26 Vitest (17 files), Pint/Larastan/`tsc -b`/eslint all clean |
| 7.5 | Dashboard polish: fixed shell, page-header-in-top-bar, themed stat cards, Payments + Applications sidebar sections | User request (post-MVP polish) | Done — fixed the sidebar/top bar staying pinned while content scrolls (`AppShell`'s outer container was `min-h-screen`, letting a tall page drag the whole document including the sidebar; changed to `h-screen overflow-hidden` so `<main>`'s existing `overflow-y-auto` actually creates its own scroll region — no `position: fixed` needed). New `lib/pageHeader/PageHeaderContext.tsx` (`PageHeaderProvider` + `usePageHeader()`) lets a page put its title/subtitle in the top bar instead of an in-body heading; applied to `AdminDashboardPage` and `PaymentsPage`/`ApplicationsPage`, other pages unchanged. New `StatCard.tsx` colors each dashboard card by meaning using only `ui-context.md`'s existing semantic tokens (success/progress/neutral/warning/danger) — no new hues. **Payments** (real, admin-only): `OrderResource` extended with `student`/`course` (was previously nested-context-only and missing both); new `Admin\OrderController::index()` at `GET /admin/orders` (unlike `EnrolmentController::index()`, not scoped to the caller — every order, filterable by status); new `OrderFactory` and `HasFactory`/relation-generics added to the `Order` model (a Phase-0-era gap, same pattern as other models earlier in the project); frontend `PaymentsPage` follows the `AuditLogPage` table pattern. **Applications** (frontend-only, explicitly per the user's request): no `pending`/`approved` state exists anywhere in the schema — enrolments auto-confirm by design (FR-2/FR-3, already tested) — so `ApplicationsPage` is a real page wired through the same hook/query shape as every other list page, backed by local mock data (`mockApplications.ts`) instead of a live endpoint, with an on-page note saying so rather than silently presenting it as connected. Both new sidebar items placed after Support/before Categories, preserving the mobile bottom nav's existing first four items. Regression: 127/127 Pest, 27/27 Vitest (18 files), Pint/Larastan/`tsc -b`/eslint all clean |
| 7.6 | View/Edit/Delete actions on Payments and Applications, 3-tab status filter on Applications | User request (post-MVP polish) | Done, **partially superseded by 7.7** — the Applications half stands as described below unchanged. The Payments half (status+payment_method update, "void via refunded status" as the delete action) was replaced by 7.7's partial-payment model; `refunded` no longer exists as a status. Original note, for history: confirmed with the user first that Applications stayed a fully-interactive **mock** (no backend change — building a real pending/rejected workflow would contradict the tested "auto-confirms, no rejection path" rule), and Payments' "Delete" **voided rather than hard-deleted** (financial records aren't destroyed for audit reasons). New shared `components/ui/Modal.tsx` (backdrop, Escape-to-close) used by both pages' View/Edit dialogs. **Applications**: extended `mockApplications.ts` with a `rejected` status and more fixtures; `useApplications.ts` gained `useSetApplicationStatus`/`useUpdateApplication`/`useDeleteApplication`, all mutating the React Query cache directly (`queryClient.setQueryData`) rather than hitting a network — same "one-line swap to a real endpoint later" shape as the original mock. Approve/Reject quick-action buttons show only on pending rows. Tabs are exactly what was asked — All / Rejected / Approved (pending rows only surface under "All", by design, matching the literal request). Regression at the time: 130/130 Pest, 30/30 Vitest (18 files), Pint/Larastan/`tsc -b`/eslint all clean |
| 7.7 | Payments: partial-payment tracking, status computed from amount paid | User request (post-MVP polish) | Done — confirmed with the user first: `refunded` status and the "void" action from 7.6 are dropped entirely (status is now just `pending`/`partial`/`paid`), and status is never set directly — the admin enters an amount paid, the backend derives the status. New `orders.amount_paid` column (migration, `amount` stays the total owed); `OrderStatus` enum narrowed to `Pending`/`Partial`/`Paid` (`failed` was already unused outside its own enum definition, confirmed by a repo-wide grep before removing it); `Admin\OrderController::update()` rewritten to clamp `amount_paid` to the order's `amount` (no negative remaining balance from an overpayment), derive status from comparing the two, set `paid_at` the first time it reaches `paid`, and audit as `order.payment_recorded` (replaces `order.status_changed`). `OrderResource` gained `amount_paid`/`remaining_balance`. Frontend: `PaymentsPage`'s three tabs now map 1:1 onto the three statuses — **Receivables** (pending: amount owed, receipt placeholder, status), **Partials** (amount owed, amount paid, remaining balance), **Paid** (amount, a plain "Payments completed" indicator, no action) — each with its own column set, a "Review payment" action (opens a modal to enter amount paid) replacing the old status-select Edit/Void actions on the first two tabs. Regression: 131/131 Pest, 30/30 Vitest (18 files), Pint/Larastan/`tsc -b`/eslint all clean |
| 7.8 | Drop the standalone Categories page (moved into the course form); Team page lists every user, not just staff | User request (post-MVP polish) | Done — frontend-only. **Categories**: removed `CategoryListPage`, the `/admin/categories` route, and its sidebar item; `CourseFormPage` gained an inline "+" control next to the Category select that opens a small name field + Add button, calling the same `createCategory` API, invalidating the `categories` query, and auto-selecting the new category — an admin never has to leave course creation to add one. The page's `deleteCategory` UI is gone with it (no page left to host it); the backend `DELETE /categories/{category}` endpoint is untouched, just unused from the frontend for now. **Team** (`ProvisionUserPage`, still at `/admin/users`): the "existing members" list previously filtered out `role === 'student'`, so it only ever showed instructors/admins even though `GET /admin/users` (no `role` param) already returns everyone — restriction was frontend-only. Rewrote it as a table of every user with role/status shown as badges (new `userRoleDisplay`/`userStatusDisplay` in `lib/statusBadge.ts`, same pattern as every other status map) and a "Manage" action button (gear icon) opening a modal to change role and/or status in one save — replaces the old always-visible inline role/status `Select` pair. Self-management stays blocked client-side (button disabled for your own row) and server-side (`UserController::update()`'s existing 422 guard, unchanged). Regression: `tsc -b` clean, ESLint clean (0 errors), 30/30 Vitest (18 files, unchanged — neither page had dedicated tests before or after). No backend changes, so Pest/Pint/Larastan weren't re-run |
| 7.9 | Payments: real student payment submissions (amount + receipt image) with admin confirm/reject | User request (post-MVP polish) | Done — closes the gap 7.7 left: until now only an admin could type in an `amount_paid`, with no way for a student to actually submit one. Confirmed with the user first on three forks: admin can **confirm or reject** a submission (not confirm-only); untouched orders (nothing submitted yet) **still show under Receivables**, distinguished from ones actually awaiting confirmation; the existing manual "Review payment" admin entry **stays** as an independent fallback (e.g. cash payments), not replaced. New `payment_submissions` table (migration + `schema.sql`) — `order_id`, `amount`, `receipt_path`/`receipt_original_name`, `status` (`pending`/`confirmed`/`rejected`), `reviewed_by`/`reviewed_at`; new `PaymentSubmissionStatus` enum, `PaymentSubmission` model/factory. `Order::deriveStatus()` extracted (was inlined only in `Admin\OrderController::update()`) so the new confirm action reuses the exact same pending/partial/paid derivation. New `App\Services\Payments\PaymentSubmissionService` — `submit()` (stores the receipt via the same `$file->store($dir, 'public')` pattern as `ForumService::storeAttachment()`, `abort_if`-guards: no exceeding the remaining balance, no submitting while one is already pending, no submitting against an already-fully-paid order — mirroring the `abort_if` business-rule pattern already used in `EvaluationAttemptService`/`ConversationService` rather than inventing a new one), `confirm()` (applies the claimed amount to `amount_paid`, re-derives status, audits `order.payment_confirmed`), `reject()` (order untouched, audits `order.payment_rejected`, and since the submission is no longer `pending`, the student can immediately resubmit). New `OrderPolicy::submitPayment` (order's own student only, mirrors `EnrolmentPolicy::withdraw`'s ownership-check shape). New routes: student `POST /orders/{order}/payment-submissions`; admin `PATCH /admin/payment-submissions/{id}/confirm`\|`/reject` (same admin gate as `Admin\OrderController::update()`). `Admin\OrderController::index()`'s `?status=` filter is no longer a raw column match — `status=pending` (Receivables) now also includes any order with a currently-pending submission regardless of its last-confirmed status (so a `partial` order awaiting its next installment's confirmation reappears there), while `partial`/`paid` explicitly exclude those until resolved; `OrderResource` gained `pending_submission` and a full `payment_submissions` history. Frontend: `MyCoursesPage` — a course card with a remaining balance now shows either a "Make a payment" button (opens `MakePaymentModal`: amount capped client-side at the remaining balance, a 5MB image-only receipt picker mirroring `ForumComposer`'s file-picker pattern) or, if a submission is already pending, an "Awaiting confirmation" badge instead. `PaymentsPage`'s Receivables tab now shows amount submitted + a real receipt thumbnail + Confirm/Check / Reject/X icon actions (mirroring `ApplicationsPage`'s Approve/Reject buttons) when a submission exists, falling back to the existing "Review payment" manual-entry action when nothing's been submitted yet; `ViewOrderModal` now shows the real receipt image and a payment history list instead of the old "coming soon" placeholder. Found and fixed the same factory-state-ordering footgun as before (`->paid()->create(['amount' => ...])` computes `amount_paid` from the state closure's pre-override attributes, not the final overridden amount) in a new test before it could cause a flaky failure. Regression: 142/142 Pest, 30/30 Vitest (18 files), Pint/Larastan/`tsc -b`/eslint all clean |
| 7.10 | Team page: table-first layout, "Add user" opens the provisioning form in a modal | User request (post-MVP polish) | Done — frontend-only, `ProvisionUserPage`. The always-visible provisioning `Card` (name/email/password/role fields) took up the top of the page above the table; moved that same form, unchanged, into a new `AddUserModal` (built on the shared `Modal`, same footer-button pattern as `ManageUserModal`/`ReviewPaymentModal` — `onClick={handleSubmit(onSubmit)}` on the footer's Create button rather than a native `<form>` submit, since the footer isn't a DOM descendant of a form element), triggered by an "Add user" button next to the page title. The "All users" table is now the first and only thing on the page by default. No behavior change to provisioning/role/status logic — same `useProvisionUser`/`useUpdateUser` hooks, same validation. Regression: `tsc -b` clean, ESLint clean (0 errors); no backend touched |
| 7.11 | Admin/instructor onboarding: invite-link accounts (no admin-typed password), email on provisioning, forced password-set before first use | User request (post-MVP polish) | Done — the user asked for two things (notify-by-email on provisioning, force a password change on first login) and explicitly asked for a best-practice recommendation first. Recommended and the user chose the **invite-link model** over "temp password + forced change": the admin no longer sets a password at all (`StorePrivilegedUserRequest` dropped the field); `Admin\UserController::store()` creates the account with an unusable placeholder (`Hash::make(Str::random(64))`), mints a token via `Password::broker()->createToken()` (the same broker/`password_reset_tokens` table `NewPasswordController` already validates against — no new consume-side logic needed), and sends a new `UserProvisionedQueued` notification (`ShouldQueue`, same "a mail hiccup can't 500 the request" reasoning as `VerifyEmailQueued`) naming their role with a "Set your password" link. **Found and fixed a real pre-existing bug** while touching this code: `AppServiceProvider::boot()`'s `ResetPassword::createUrlUsing` built a link to `/password-reset/{token}?email=...`, but the frontend route is `/reset-password` with `token`/`email` as query params (`ResetPasswordPage.tsx`) — every "forgot password" email before this fix 404'd on click. Extracted the correct shape into a new `App\Support\PasswordResetUrl::for()`, used by both the fixed `AppServiceProvider` closure and `UserProvisionedQueued`. `NewPasswordController::store()` now also marks the email verified inside its reset callback (clicking a single-use emailed link is itself proof of inbox ownership — a no-op for an already-verified self-service reset). Also wired up `users.last_login_at`, a column that existed and was cast on the model but was never actually written anywhere (`AuthenticatedSessionController::store()` now sets it on every successful login) — this doubles as the signal the Team table uses for a new "Invited" badge (shown when a non-student's `last_login_at` is still null) with no additional schema needed. New Pest coverage in `UserManagementTest.php` (no-password provisioning queues the notification, password stays unusable until reset, completing the token flow logs in and verifies email) and `AuthenticationTest.php` (`last_login_at` populated after login) — hit a test-harness quirk along the way: `actingAs()` re-authenticates on every subsequent guard resolution regardless of session state, so a real `/logout` call mid-test can't undo it; worked around by testing the token-consume behavior in an isolated `it()` that doesn't chain an authenticated admin action beforehand. Frontend: `AddUserModal` lost its "Temporary password" field entirely (name/email/role only, with a line explaining the invite email), `UserResource`/`User` type gained `last_login_at`. Regression: 145/145 Pest, 30/30 Vitest (18 files), Pint/Larastan/`tsc -b`/eslint all clean |
| 7.12 | Support tickets: table + side-panel respond view for staff, `/tickets/:id` retired | User request (post-MVP polish) | Done — frontend-only. Confirmed with the user first: instructors get the same treatment as admins (both already view/respond/update status via the identical `TicketPolicy::manage`-gated endpoints); students keep their existing card-list + "New ticket" flow unchanged; the standalone `/tickets/:id` page is retired (same precedent as dropping `/forum-threads/:id`, tracker 5.6/5.7) but redirects to `/tickets?ticket={id}` with that ticket pre-opened rather than just disappearing. New `TicketConversation.tsx` extracted from the old `TicketDetailPage.tsx` body (staff status/assign controls, message thread, reply form) — takes an already-loaded `ticket` prop instead of fetching itself, so it's reusable as both the staff side panel's content and the student modal's content without duplicating the thread/reply logic. `TicketsPage.tsx` now branches on role: staff see a table (student, subject, status) with a quick inline status `Select` per row (immediate `PATCH`, no confirmation — mirrors the Team page's original inline-control pattern) and a View action that opens a right-side panel; the table narrows to `lg:w-2/5` and the panel takes `lg:w-3/5` on wide screens (a real split view, not an overlay), while on narrow screens the table hides and the panel takes over full-width with its own close button acting as "back" (same narrow-screen-stacking precedent as 6.5's other dense-table fixes). Students keep their card list, but clicking a card now opens `TicketConversation` in the shared `components/ui/Modal.tsx` instead of navigating to the removed route, and creating a ticket opens it directly rather than navigating. New `TicketRedirect.tsx` handles the retired `/tickets/:id` route. Along the way, `tsc -b` surfaced a real pre-existing gap from 7.11: `User` mock fixtures in six unrelated test files (`ProfileMenu.test.tsx`, `AccountPage.test.tsx`, `AdminDashboardPage.test.tsx`, `CourseCard.test.tsx`, `HeroSection.test.tsx`, `ForumPage.test.tsx`, `CoursePlayerPage.test.tsx`) had never been updated with the new required `last_login_at` field — fixed all of them. Regression: `tsc -b` clean, ESLint clean (0 errors), 30/30 Vitest (18 files); no backend touched |
| 7.13 | Student dashboard: drop the dedicated Announcements section, route announcements through notifications; notifications labeled by type | User request (post-MVP polish) | Done — frontend-only; the backend already fanned announcements out as in-app notifications (`NotificationDispatcher::notifyAnnouncementPosted()`, type `announcement_posted`, since 5.8/5.9), so no backend change was needed — the student-facing gap was purely that `AnnouncementsPanel` was *also* embedded as its own section in `CoursePlayerPage.tsx`, duplicating what the bell already delivered. Removed that embedding (staff still manage announcements via the same `AnnouncementsPanel` in `CourseBuilderPage.tsx`, untouched — create/delete was never reachable from the student-only course player anyway, so nothing staff-facing was lost). New `notificationTypeDisplay()` in `lib/statusBadge.ts` mapping every `NotificationDispatcher` type string (`announcement_posted`, `course_updated`, `certificate_issued`, `new_message`, `ticket_reply`, `forum_reply`, `grade_posted`, `module_unlocked`) to a label/tone/icon, with a generic fallback for any future type added server-side without a matching frontend entry — same pattern as every other status map in the file, but keyed by a plain string instead of a strict union since `AppNotification.type` isn't one. `NotificationBell.tsx` now renders that as a small `Badge` above each notification's title, so "Announcement" (and every other type) is visually labeled rather than inferred from wording alone. Regression: `tsc -b` clean, ESLint clean (0 errors), 30/30 Vitest (18 files, confirmed after a transient worker-pool timeout on the first run reran clean) — no backend touched |
| 7.14 | Forum refactor: threaded Discord/Slack/GitHub-Discussions-style discussions | External spec, full refactor (post-MVP) | Done — the biggest single change this project: research first confirmed the existing `ForumThread` + `headPost()` (`hasOne(...)->oldestOfMany()`) model already *is* "a discussion + its opening message, everything else a reply," so this was an additive-schema + full-UI pass, not a data migration — existing threads' auto-derived `title` (from the earlier feed redesign) just became real/shown again, no backfill needed. Confirmed with the user first on three forks: **rich text** uses new dependencies (`react-markdown`, `remark-gfm`, `react-syntax-highlighter` — none existed before) rather than a hand-rolled formatter, since real syntax highlighting needs a real library; **tags** are in scope now (small new table), **emoji reactions and @mentions are explicitly deferred** (mentions in particular needs net-new parsing/autocomplete infrastructure that doesn't exist anywhere in this codebase); and since this project has **no shadcn/ui at all** (confirmed: no `components.json`, no Radix, no `cva` — every `components/ui/*` file is hand-rolled Tailwind), new primitives (`Tooltip`, `DropdownMenu`, `Skeleton`) were built in that same hand-rolled style rather than shadcn-generated, per the spec's own "keep design consistent with the LMS" clause winning over "use shadcn/ui." **Backend** (all additive): `forum_threads` gained `solved`/`last_activity_at` (bumped on replies, not edits); new `forum_tags`/`forum_thread_tag` (global, not course-scoped — the example vocabulary is generic/technical, not tied to one course) and `forum_thread_reads` (a genuinely new small table beyond the spec's own "possible additions" list, but necessary for the *required* — not optional — unread indicator); no `edited_at` column added since `forum_posts.updated_at` already carries that signal (`ForumPostResource` computes `edited` from `created_at !== updated_at` — found and fixed a real same-second test flakiness this exposed, using `$this->travel()` like elsewhere in the suite). `ForumThreadController::index()` moved from an unpaginated `->get()` to `->paginate(20)`; search now also matches the (now-real) title alongside the existing full-text post-body search; new `?sort=`/`?tags[]=` params; new `GET /forum-threads/{thread}/posts` paginates replies separately (the old single-response `posts.user` eager load is gone) and `GET /forum-tags` feeds the composer's tag autocomplete. Marking a discussion solved is staff-only per the spec's own per-role action list (not the author) and notifies via a new `notifyForumThreadSolved()`. **Frontend**: `ForumPage.tsx` is now a searchable, infinite-scrolling (`useInfiniteQuery` + `IntersectionObserver`), pinned-then-date-grouped discussion list with client-side search-term highlighting, reusing the exact list-narrows/panel-opens split-view pattern already built for Support tickets (7.12) rather than inventing a new one; opening a discussion renders the new `DiscussionThread.tsx`, which deliberately does **not** use WhatsApp-style left/right alternating bubbles — the spec explicitly asked to avoid that, and its own worked example shows uniform Discord/Slack-style rows (avatar + name + timestamp), so messages are rendered that way with just a subtle tint on the current viewer's own messages. New `lib/markdown/MarkdownContent.tsx` (render) and `MarkdownComposer.tsx` (a formatting toolbar inserting Markdown at the cursor + live preview) are shared by both the discussion composer and the reply box. Deleted the now-fully-superseded `ForumPostCard.tsx` and rewrote `ForumComposer.test.tsx`/`ForumPage.test.tsx` against the new component APIs. Regression: 154/154 Pest, Pint/Larastan clean, `tsc -b` clean, ESLint clean (0 errors), 30/30 Vitest (18 files) |
| 7.15 | `DatabaseSeeder`: demo data across every table in `schema.sql` (~10 rows each) | User request ("seed all the tables... like 10 records each") | Done — rewrote `DatabaseSeeder.php` from the original 3-user/1-course stub into ~50 private `seedX()` methods covering every table, run in FK-dependency order (users → categories/courses → modules/resources → assignments/evaluations → enrolments/orders/payments → messaging/tickets/announcements → forum → notifications/engagement/audit). Used each model's existing factory where one exists; hand-assembled the rest directly against the model's own `$fillable` for tables with no factory (`module_items`, `resource_progress`, `video_watch_pings`, `module_progress`, `plagiarism_reports`, `evaluation_attempt_answers`, `assignment_submission_rubric_scores`, `oauth_accounts`, `course_change_logs`) rather than inventing new factories for one-off seed rows. Orders are derived from enrolments with `amount` always equal to the enrolled course's `price` (per the payment-flow rule from 7.9: a student never owes more than their course costs), cycling pending/partial/paid so all three Payments tabs have real data. Forum data goes through `ForumService::createThread()`/`reply()`/`markThreadSolved()`/`markThreadRead()` rather than the `ForumThread`/`ForumPost` factories directly, so `last_activity_at`, tag sync, and reply notifications behave exactly as they do for a real post instead of needing to be faked separately. Hit and fixed one real bug: several `seedX()` methods built their collection via the `collect()` helper (which always returns `Illuminate\Support\Collection`) but were type-hinted to return `Illuminate\Database\Eloquent\Collection`, causing a `TypeError` at runtime — fixed by seeding from `new Collection(range(...))` instead, so late static binding keeps the Eloquent subclass through `->map()`. Ran `./vendor/bin/phpstan analyse database/seeders/DatabaseSeeder.php` directly first and saw 162 errors, but confirmed `phpstan.neon`'s `paths` only covers `app/` — seeders were never in the project's actual Larastan scope, so the real verification command (`./vendor/bin/phpstan analyse` with no path override) reports clean, matching every other file under `database/`. Verified row counts post-seed via `DB::table($t)->count()` across all 51 tables — every table has data (`users` 17, `resources` 14, `forum_posts` 23 including replies, `notifications` 27 including real side-effect notifications from forum replies/solves, everything else at or near the requested ~10). Pint clean, Larastan clean (`app/` scope) |
| 7.16 | Course Builder redesign: table-driven modules, embedded analytics, announcements via the notification bell | User request + mockup image (post-MVP polish) | Done — full plan-mode task with four upfront confirmations. **Groups removed from the UI entirely** (module creation dropped its "restrict to groups" checkboxes too) — backend/schema (`groups_cohorts`, `module_groups`, `GroupPolicy`) untouched, just unreachable from the UI; deleted `GroupsPanel.tsx` and the now-fully-unused `useGroups`/`useCreateGroup`/`useDeleteGroup`/`useAddGroupMember`/`useRemoveGroupMember` hooks + their `api.ts` functions. **Announcements moved into the notification bell**: new `AnnouncementComposer.tsx` (course `<select>` — admin sees `useCourses({})`, instructor sees `useCourses({ instructor_id: user.id })`, which `CourseController::index()` already resolves to exactly "courses I teach" for an instructor caller, zero backend change — then create/list/delete for the selected course, same JSX shape as the deleted `AnnouncementsPanel.tsx`) toggled from a new Megaphone icon in `NotificationBell.tsx`'s header, staff-only. **At-risk data got real, not decorative, additions**: `AnalyticsService::courseAnalytics()` now returns `final_grade_percent` per at-risk student (reusing `GradebookService::forCourse()`'s existing formula, not a new one) and a `risk_factor` computed from a real light heuristic — `'No activity'` (never engaged), `'Assignment backlog'` (has an overdue assignment with no submission), else `'Inactive'` — plus a new `roster` array (every confirmed enrolment's `percent_complete`, reusing `ProgressEngine::applicableModules()` per student the same way `ProgressController::dashboard()` already does, just iterated course→students instead of student→courses, and `status: active|graduated`). Extracted the at-risk filter into a shared private `atRiskEnrolments()` so it isn't duplicated. **"Send Mass Notice" built for real** (it was only in the mockup, not the text ask — confirmed with the user first): new `POST /courses/{course}/at-risk-notice` (`AnalyticsController::notifyAtRisk`, same `viewAnalytics` gate) sends a real in-app `at_risk_reminder` notification (`NotificationDispatcher::notifyAtRiskReminder()`) to every currently at-risk student — corrected mid-plan from an initial "in-app + email" framing once `NotificationDispatcher`'s own class doc confirmed email/SMS/push fan-out is explicitly un-built "Phase 5." **Module Management is now a table**: `ModuleCard.tsx` refactored into `ModuleTableRow.tsx` (same state/handlers, rendered as `<tr>` + a conditional expanded `<tr>` instead of a `<Card>`) — Module Name, a real status badge (`Active` vs. the existing `Opens {date}` warning badge, not a fabricated "Draft" state modules don't have), and three action icons (resource/assignment/evaluation) that open the existing `ResourceForm`/`AssignmentQuickForm`/`EvaluationQuickForm` in `components/ui/Modal.tsx` instead of an inline expand — clicking the row still expands the existing item list/delete-module capability, so nothing from the old card was lost. New `AtRiskStudentsTable.tsx` and `EnrollmentTable.tsx` (client-side name/email filter; a real CSV export built from the already-loaded `roster` via `Blob`/`URL.createObjectURL`, no backend endpoint or new dependency). `CourseBuilderPage.tsx` rewritten to the mockup's two-column layout (stat cards + Module Management table, then the two new tables full-width below) using this app's existing `Card`/`Badge`/`ProgressBar`/raw-`<table>` design system rather than the mockup's specific colors — same precedent as overriding the forum spec's "use shadcn" instruction — and dropped the mockup's fabricated "+12%" trend chip since there's no historical snapshot to compute a real one from. **Standalone analytics page retired**: `AnalyticsDashboardPage.tsx` (+ its test, + the `/admin/courses/:id/analytics` route) deleted now that its content is embedded here, same "don't leave two views of the same thing" precedent as dropping `/forum-threads/:id`/`/tickets/:id`. Regression: 159/159 Pest (8 new `CourseAnalyticsTest.php` cases), Pint/Larastan clean, `tsc -b` clean, ESLint clean (0 errors), 30/30 Vitest (17 files) |
| 7.6 | Payments & Applications: view/edit/void/delete actions, Applications approve/reject + 3-tab filter | User request (post-MVP polish) | Done — new shared `components/ui/Modal.tsx` (backdrop + Escape-to-close), the first modal primitive in the app, used by both pages below. **Payments**: confirmed with the user that financial records shouldn't be hard-deleted, so "Delete" is a two-click-confirm **void** action that sets `status` to `refunded` rather than removing the row (`Admin\OrderController::update()`, new `PATCH /admin/orders/{order}`, gated the same way as `index()`, audited as `order.status_changed` via the existing `AuditLogger` — same pattern as `AdminUserController::update()`'s role/status changes). Edit modal changes status + payment method; View modal shows full order detail including the previously-unexposed `provider_ref` (now added to `OrderResource`). **Applications** (still frontend-only mock, confirmed with the user — building a real backend would contradict the tested "auto-confirms, no rejection path" business rule): added a `rejected` status alongside `pending`/`approved`; Approve/Reject/Edit/Delete are all genuinely interactive, mutating the TanStack Query cache directly (`useSetApplicationStatus`/`useUpdateApplication`/`useDeleteApplication` in `useApplications.ts`) rather than the network — same one-line-swap-later shape as before, just now with working mutations instead of a static list. Three tabs (All/Rejected/Approved) filter the table client-side, matching exactly what was asked (pending items are only visible under "All," by design — there's no separate Pending tab). Regression: 130/130 Pest, 30/30 Vitest (18 files), Pint/Larastan/`tsc -b`/eslint all clean |

---

## Open questions still needing a product decision

Carried over from the PRD — not blocking Phase 0–1, but should be resolved before the phase
that depends on them starts:

- **Content authoring model** (Phase 2): raw file upload only, or in-browser lesson/quiz builder?
  Affects 2.6/2.7/3.6 scope significantly. Directly bears on the gap noted in 3.14: question
  banks/questions and assignment rubrics currently have no create/edit UI (API-only) — whatever
  gets decided here likely governs how much of that authoring surface actually needs building.
- ~~**Question bank pull strategy** (Phase 3): fixed question set per evaluation vs. random pull
  from a bank per attempt — `evaluations.questions_per_attempt` supports either, needs a decision
  before 3.7.~~ Resolved during 3.7: implemented as "supports either" rather than picking one —
  an evaluation attaches its full `question_ids` set via the `evaluation_questions` pivot, and
  `randomize_questions`/`questions_per_attempt` optionally shuffle/subset that set per attempt in
  `EvaluationAttemptService::questionsFor()`.
