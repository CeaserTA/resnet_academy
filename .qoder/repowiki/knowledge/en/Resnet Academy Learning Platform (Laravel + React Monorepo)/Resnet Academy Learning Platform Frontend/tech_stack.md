React 19 + TypeScript on Vite 8, styled with Tailwind CSS v4 and class-variance-authority; data fetching via @tanstack/react-query with Axios; rich-text editing via TipTap (starter-kit + code-block-lowlight, color, highlight, image, link, table, youtube extensions); form validation with react-hook-form + zod; routing with react-router v8; UI primitives built on Radix UI and Base UI; testing with Vitest (unit) and Playwright (e2e).